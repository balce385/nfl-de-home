// Bezahl-Funktion entfernt — diese Komponente wird nicht mehr verwendet.
// Datei kann manuell gelöscht werden.
export function ProCheckoutButton({ children }: { children: React.ReactNode }) {
  return <span>{children}</span>;
}
